# 「纸上交锋 · PAPER STRIKE」复刻 Prompt

把下方分隔线之后的全文交给实现 agent。这是完整的单文档规格，不依赖参考图片、附件、原项目源码或历史对话。提到的文件名都是需要创建的交付物。

---

请在工作目录实现一个完整可玩的网页 3D 第一人称射击游戏「纸上交锋 · PAPER STRIKE」。单人模式是单关卡清剿，另有一个同局域网 1v1 对战模式。直接完成代码、依赖安装、构建、测试和浏览器验证，不停留在静态画面或计划。保留目标目录中无关的用户文件。

## 1. 核心目标

粗糙草图笔触、可爱几何体角色、鲜明暖黄／橘红／青绿配色必须同时成立。全程使用 Three.js 基础几何体与 Canvas/SVG/CSS 作画，不调用 AI 生图、不下载图片或模型。

技术栈固定为 HTML + JavaScript ES Modules + Three.js 0.186.0 + Vite 8.3.0，不使用任何 UI 框架。

**纯前端硬约束**：不要后端、不要 API key、不要环境变量、不要数据库。安装 npm 依赖后，游戏运行期不请求任何第三方 CDN、分析服务或游戏后端——单人模式必须做到全程零外部请求。页面只从当前站点取构建资源。

两个模式：

| 模式 | 场地 | 目标 |
| --- | --- | --- |
| 单人 `solo` | 8 名 AI 敌人 | 清空全部敌人即胜利，玩家生命归零即失败 |
| 双人 `duel` | 空场地，0 名 AI 敌人 | 两名真人各自一台电脑、各自视角，1v1 单挑 |

**本期的范围边界，请严格遵守**：联机部分只做到**通道打通**——两端能建链、能互发报文、能进入各自的决斗场地，但**不做位置同步、不做命中判定同步**，两个玩家不会在对方屏幕上出现。这不属于本期交付，不要顺手实现。同理，**不要**加入蹲下、奔跑、回血、防具、武器掉落与拾取、多武器槽、波次生存、账号、商店、存档或移动触控。

## 2. 美术方向：粗糙草图，颜色鲜明

### 造型与画法

- 场景、角色、手臂和武器使用方块、圆柱、球体、椭球、平面等基础几何体搭建。
- 采用涂鸦本、彩铅草稿、手工纸板的感觉，允许画歪、断笔、线头越界、重复描边和没有完全填满的色块。
- 用深色轮廓线、少量排线、随手涂色、歪斜字牌和绘制的螺丝／接缝体现草稿感。
- 在面上绘制五官、表情、窗户、木箱叉线、标牌、涂鸦和纸张笔触。装饰简洁、有辨识度，不堆砌精细机械零件。
- 线条的不规则偏移应固定或使用固定随机种子，不能每帧随机闪动。
- 粗糙感主要来自线条和涂色方式；大色块要清晰、饱满，画面不要整体泛白、发灰或被米黄色雾效洗淡。
- 不做写实军事美术、金属反射、精致商品渲染或过度光滑的塑料模型。

### 资产生成方式

- **不调用 AI 生图，不下载现成角色、枪械、场景模型或图片。**
- 三维形体由代码构建，贴图由 Canvas 2D 绘制，界面图形可用 SVG / CSS。
- 不用一张图片冒充可交互三维场景。
- 音效使用 Web Audio 合成，不依赖外部音频。不要引入任何音频文件。
- 不从 CDN 加载引擎、字体或媒体。

### 最终色卡

以下是需要复刻的最终配色，不采用浅粉、浅米、浅灰绿覆盖整幅画面的方案：

| 用途 | 色值 |
| --- | --- |
| 场景轮廓／墨线 | `#293331` |
| 奶油色建筑 | `#F3E5C4` |
| 沙色地面与围墙 | `#DBCAA6` |
| 橘红建筑 | `#CF7352` |
| 青绿建筑与掩体 | `#659A99` |
| 蓝色细节 | `#91B6BC` |
| 暖黄标记 | `#F4BD55` |
| 深色结构 | `#394B4B` |
| 暖白留白 | `#FFF4D8` |
| 天空 | `#B9D6DD` |
| 远景雾色 | `#C4DADD` |
| UI 主按钮／强调色 | `#E66B43` |
| UI 深色文字 | `#263D39` |
| UI 纸色底板 | `#F5F1E8` |
| 肤色 | `#F6D6A5` |
| 腮红 | `#E9A08A` |

场景应明显看出蓝天、暖黄木箱、橘红楼房和青绿墙体。保留局部纸底留白，但填色主体使用足够高的不透明度。

### 光照、雾与相机

使用简单卡通明暗和适量阴影建立空间关系。以下数值可直接采用，再在实际浏览器里核对颜色：

- `HemisphereLight('#FFF9E6', '#778B8A', 2.55)`。
- `DirectionalLight('#FFF5DB', 3.0)`，位置约 `(-47, 87, 55)`，投射阴影；阴影贴图 `2048×2048`，正交范围 `±46`，`near 30 / far 220`，`bias -0.0012`，`normalBias 0.045`。
- 补光 `DirectionalLight(#91B6BC, 0.5)`，位置约 `(30, 18, -30)`。
- 线性雾 `Fog(#C4DADD, 43, 100)`——不要让近景蒙上淡色雾。
- 主相机 `PerspectiveCamera(75, aspect, 0.08, 600)`，`rotation.order = 'YXZ'`。准备页另有 `MENU_FOV = 48` 的展示镜头。
- 使用 sRGB 输出。

## 3. 角色：可爱的大头小豆人

敌人虽然会交战，造型必须友好、呆萌，像涂鸦本里的小玩偶。

- 约两头身比例：大圆脸、豆子形身体、短腿、小圆鞋、圆手和软袖子。
- 帽子是带两个圆耳朵的软帽；脸颊圆润，有腮红。
- 脸部使用小点眼／眯眼、小鼻子、小小的笑嘴或惊讶嘴，不画凶眉、咬牙表情或写实军人五官。
- 身体前方有简单浅色肚兜／补丁和两颗小纽扣，手持小巧纸板玩具枪。
- 移动时有小幅摇摆和短腿摆动，搜索时可微微歪头；警戒提示使用手画气泡中的 `!` / `?`。
- 保持可爱，不加血腥表现。

模型比例，单位为米（括号内为相对父节点的局部坐标）：

- 豆子躯干椭球中心 `(0, 0.79, 0)`，半径 `(0.36, 0.43, 0.29)`。
- 头组 `headGroup` 位于 `(0, 1.53, 0)`，头椭球半径 `(0.43, 0.45, 0.38)`。
- 帽子相对头部 `(0, 0.16, 0)`：帽顶为半球再缩放 `(0.45, 0.4, 0.42)`，帽檐为圆柱缩放 `(0.46, 0.05, 0.43)`，两只圆耳椭球 `(0.135, 0.135, 0.12)` 位于 `(±0.3, 0.3, 0.02)`。
- 手臂位于 `(±0.34, 1.0, 0)`，袖子胶囊半径 `0.1`、高 `0.24`，手为半径 `0.12` 的椭球，`rotation.z = side * 0.18`。
- 腿位于 `(±0.17, 0.42, 0)`，胶囊半径 `0.105`、高 `0.34`，鞋椭球 `(0.15, 0.1, 0.19)`。
- 玩具枪位于 `(0.42, 0.72, -0.16)`，枪身 `0.09 × 0.1 × 0.34`，枪管半径 `0.035`、长 `0.3`。
- 表情贴在头部前侧，优先使用与头形一致的曲面贴片，避免明显悬空的五官平板。
- 碰撞半径 `0.49`、身高 `1.9`（见第 4 节与第 5 节）。规划路径时再保留少量余量，防止大脸和圆手穿进墙体。

三套外观，按敌人在列表中的下标轮换：

| 项目 | 取值 |
| --- | --- |
| 衬衫色 | `#D56A45` / `#659DAE` / `#F0BF55`，下标 `% 3` |
| 表情 | `smile` / `squint` / `dazed`，下标 `% 3` |
| 帽子 | `#73A29A` |
| 肤色 | `#F6D6A5` |

保持颜色清楚，避免全部变成很淡的粉彩。

## 4. 地图：日光街区

### 边界与围墙

可玩区域 `ARENA = { minX: -23, maxX: 23, minZ: -25, maxZ: 25 }`，即 46 × 50 米、2300 平方米。地面平面的实际网格比可玩区各向外多 40 米，贴图按 8 米重复一次。

四周有高 4.5 米的沙色围墙，位于 `z = ±25.5`（厚 1、长 48）与 `x = ±23.5`（厚 1、长 52），把场地封闭。

### 建筑与掩体

三栋地标建筑加一座补给棚，均以「中心 x, 中心 z, 宽 × 高 × 深」给出；每栋顶部有一层不可站立的屋顶板：

| 位置 | 尺寸 | 材质 |
| --- | --- | --- |
| 西侧建筑 `(-12, -0.5)` | `10 × 5.5 × 8` | 青绿 `#659A99` |
| 东侧建筑 `(12, -11.5)` | `9 × 5 × 8` | 橘红 `#CF7352` |
| 北侧建筑 `(-4.5, -21.5)` | `13 × 6 × 7` | 奶油色 `#F3E5C4` |
| 南侧补给棚 `(-21.5, 16)` | `3 × 3.4 × 6` | 奶油色 |

东侧高台与坡道：

- 高台 x `10…18`、z `-4…1`，高 `2.5` 米，混凝土蓝灰 `#91B6BC`。台面边缘加一圈黄色警示条（不参与碰撞）。
- 坡道 x `11…15`（中心 13、宽 4），从 `z = 9` 的地面连续升到 `z = 1` 的台面，由 **8 级台阶**构成，每级深 1 米，高度依次为 `0.3125 / 0.625 / 0.9375 / 1.25 / 1.5625 / 1.875 / 2.1875 / 2.5`。视觉上是阶梯坡道，不是连续斜面。**坡道是上高台的唯一通路**，高台侧面必须走不上去。

中央庭院与各处的掩体：

- 玩家出生点正前方木箱 `(0, 15.6)`，`2.6 × 1.05 × 1.1`。
- 左右两片屏风墙 `(±3.4, 17.2)`，各 `0.5 × 2.3 × 4.2`（左青绿、右橘红）。
- 沙色矮墙 6 段：`(0, -3.0)` `7 × 1.0 × 0.6`、`(5.5, -12.5)` `0.6 × 1.1 × 5`、`(-6.5, -16.5)` `5 × 1.0 × 0.6`、`(8.5, 9.5)` `0.6 × 1.05 × 4`、`(17, -20.5)` `5 × 1.1 × 0.6`、`(-20.6, -2.0)` `0.6 × 1.0 × 5`。
- 橘红拱门：两根柱 `(-5.6, -9.0)` 与 `(-2.6, -9.0)`，各 `0.9 × 3.6 × 0.9`；门楣 `(-4.1, 3.6, -9.0)`，`4.0 × 0.7 × 1.1`。
- 木箱 14 个，分布于 `(3.6, 11.5)`、`(5.0, 12.4)`、`(3.9, 11.6)`（叠在第一个上面）、`(-5.2, 9.5)`、`(-6.6, 10.7)`、`(-2.0, 1.5)`、`(-2.0, 1.45)`（叠加）、`(5.2, -6.5)`、`(6.6, -5.6)`、`(0.5, -13.0)`、`(9.2, 6.5)`、`(-20.8, 4.0)`、`(-14.0, -21.5)`、`(20.4, 14.2)`，边长约 `1.1 ~ 1.6`。
- 油桶 8 个（圆柱半径 `0.45`、高 `0.95`）：`(-6.2, -3.2)`、`(7.0, -9.0)`、`(1.2, 7.6)`、`(-1.6, -17.4)`、`(10.5, 11.0)`、`(-16.0, -14.5)`、`(16.9, 8.6)`、`(19.8, -6.5)`。
- 仙人掌 4 个（青绿偏绿 `#6f9e6a`）：`(-21.6, 22.5)`、`(21.6, 20.5)`、`(-21.9, -6.5)`、`(21.4, -13.0)`，尺寸 `0.9 × 2.3 × 0.9` 上下。
- 电线杆 3 根（`0.3 × 5.2 × 0.3`，木色 `#c9a877`）：`(-3, 8)`、`(6.5, -17.5)`、`(20, 10)`。

### 出生点

- 玩家 `PLAYER_SPAWN = (0, 0, 19)`，朝 `-Z`，身前有木箱与屏风墙保护。
- 敌人 8 个：`(-16.9, 6.4)`、`(-17.2, -11.1)`、`(-2.4, -10.4)`、`(4.4, -1.0)`、`(18.0, -16.8)`、`(4.9, -20.8)`、`(-13.3, -16.8)`、`(19.1, 5.4)`。开局时全部就位，不做分批入场。
- 决斗两点 `(-6, 0, 17)` 与 `(6, 0, -17)`，**横向错开**、间距约 36 米，朝向互相面对（`yaw = atan2(-12, 34)` 与 `atan2(12, -34)`）。**这两个点之间不能有直线视线**，否则开局就是互狙——必须写测试守住（见第 10 节）。

### 材质与描边

材质按用途命名复用：`wallSand`（沙色围墙贴图 + `#e4d3ac` 叠加）、`wallCream` / `wallTeal` / `wallOrange`（对应基色的墙面贴图）、`concrete`（蓝灰贴图 + `#dfe9ea` 叠加）、`crate`、`barrel`、`hazard`（黄黑条纹）、`cactus`、`wood`。

描边用 `MeshBasicMaterial(#293331, side: BackSide)` 反向放大一层；草稿线用 `LineBasicMaterial(#293331)`，主描边不透明度 `0.6`、回响线 `0.3`。共享材质、几何体与贴图，不要每个物体各建一份。

### 导航图

导航图用 `buildNavGraph` 离线构建，供 AI 寻路：

- 网格边长 `1.0`，掩体间隙 `clearance = 0.62`，身体高度 `1.85`，台阶容差 `0.45`。
- 范围 `x ∈ [-21.6, 21.6]`、`z ∈ [-23.6, 23.6]`，即 44 × 48 个节点。
- 节点落在被胶囊体阻挡的位置就剔除；连通用 8 邻域，仅当两端高差 `≤ 0.5` 且连线无遮挡时才连边，斜向边代价乘以 `√2`。
- 建图后**只保留最大连通分量**，避免 AI 走进孤岛。
- 寻路用 A*（启发式 `hypot + |dy| * 1.5`），再对结果做直线化平滑（高差 `≤ 0.55` 且连线无遮挡才合并）。
- `world.test.js` 必须覆盖：八个出生点都能到达玩家入口、都能经坡道到达东侧高台、坡道之外无法走上高台、场地封闭不能穿出。

## 5. 玩家与操作

| 操作 | 按键 |
| --- | --- |
| 移动、观察 | WASD、鼠标 |
| 射击 | 左键 |
| 瞄准 | 按住右键（狙击枪进入 4 倍镜） |
| 跳跃 | Space |
| 换弹 | R |
| 切换武器 | 1 / 2 |
| 暂停 | Esc |
| 内嵌浏览器兼容观察／射击／瞄准 | 拖动鼠标或方向键 / F / T |

物理与手感常量：

| 项目 | 值 |
| --- | --- |
| 生命 | 100 |
| 步行速度 / 瞄准速度 | `5.5` / `3.25` 米每秒 |
| 跳跃初速 | `6.4` 米每秒 |
| 重力 | `18` 米每秒平方 |
| 地面加速度 / 空中加速度 | `46` / `13` |
| 地面摩擦 / 空中摩擦 | `42` / `4` |
| 最大下落速度 | `46` 米每秒 |
| 碰撞半径 / 身高 / 眼高 | `0.33` / `1.8` / `1.68` 米 |
| 台阶容差 | `0.35` 米 |
| 俯仰角上限 | `±85°` |
| 瞄准灵敏度倍率 | `0.45` |

- 玩家从自身眼高位置发射射线做命中判定，不能从摄像机位置凭空扣血。
- 支持墙体滑动、薄墙防穿透、箱顶着陆、坡道连续高度和高台侧面阻挡。碰撞用子步长积分，单步不超过 `1/120` 秒或 `0.1` 米，避免高速穿墙。
- 走路时第一人称视角有轻微上下晃动，晃动幅度随速度归一化。
- 受击添加约 `0.2` 秒轻微视觉晃动，**不永久修改逻辑瞄准**。
- **玩家不做回血**，整局生命只减不增。

## 6. 两把武器

| 武器 | 射击 | 间隔 | 弹匣 / 换弹 | 身体 / 爆头伤害 | 腰射 / 瞄准散布 | 倍率 | 切枪 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 蜂鸟 · 冲锋枪 `smg` | 按住连射 | `0.1` 秒 | `30` / `1.5` 秒 | `20` / `40` | `0.022` / `0.006` | 轻微放大 `1.15×` | `0.35` 秒 |
| 长鸣 · 狙击枪 `sniper` | 每次按下一发 | `1.2` 秒 | `5` / `2.2` 秒 | `100` / `200` | `0.032` / `0.00035` | 4 倍镜 | `0.5` 秒 |

- 备弹无限，但仍需换弹。HUD 用 `∞` 表示。
- 移动中散布放大 `1.35` 倍。
- 4 倍镜按投影放大公式计算：`2 * atan(tan(baseFov / 2) / 4)`，即约 **21.7°**。开镜时降低灵敏度（按 `tan(ads/2) / tan(base/2)` 换算），并隐藏枪身。
- 换弹时退出瞄准显示；重复换弹请求不重置进度；切枪取消未完成的换弹并**保留弹量**，不能通过切枪跳过冷却。
- 打空自动换弹；满弹匣不能重复补弹；空枪点击触发空响并强制换弹。
- 爆头伤害恰为身体伤害的 2 倍。只有弹丸类武器区分爆头。

两把武器都要有可辨识的第一人称纸板模型与 Web Audio 音效：

- 冲锋枪：纸板机匣 `0.1 × 0.13 × 0.4`、青绿准星导轨、青绿枪管、橘红枪口与握把、纸板弹匣、枪尾配重，贴纸写 `pew pew!`。开镜时准星淡出。
- 狙击枪：更长的纸板机匣 `0.085 × 0.11 × 0.5`、青绿长枪管、暖黄枪口、蓝灰瞄准镜筒配深色镜环与两片镜座、橘红握把、带黄铜色拉栓。整体缩放 `0.86`，贴纸写 `loooong!`。

动作表现：开火后坐（狙击枪幅度更大）、枪口闪光、狙击枪拉栓动画、换弹下沉、切枪下坠再抬起、行走晃动、开镜平滑插值。武器必须使用**独立的视图层与独立相机**渲染，保证枪械自身遮挡正确且不穿入墙面。

## 7. 八名敌人

| 属性 | 值 |
| --- | --- |
| 数量 | 8（对应 8 个出生点） |
| 生命 | `80` |
| 命中玩家伤害 | `10` |
| 巡逻 / 追击速度 | `2.4` / `4.0` 米每秒 |
| 搜索 / 后撤速度 | `3.4` / `1.92` 米每秒 |
| 转向速度 | `3.6`（各状态另有 `2.6 ~ 6.0` 的覆盖值） |
| 攻击距离 / 偏好距离 | `24` / `11` 米 |
| 察觉距离 / 视野角 | `26` 米 / `62°` |
| 首次预警 / 交火预警 | `0.85` / `0.6` 秒 |
| 射击冷却 | `2.3 ~ 3.6` 秒随机，下限 `1.2` 秒 |
| 命中率 | 近 `0.53` → 远 `0.23` 线性插值 |
| 记忆时长 | `7` 秒 |
| 死亡动画 | `1.15` 秒 |
| 同伴间距 | `1.02` 米 |

AI 是一个五状态机：`patrol` / `alert` / `attack` / `search` / `dead`。

- `patrol`：在最大连通分量里随机取巡逻点（要求距出生点 `3 ~ 16` 米，最多试 12 次，失败则回出生点），沿路径走去，走完停 `0.6 ~ 2.2` 秒。
- `alert`：发现玩家后先转向并显示 `!` 气泡预警 `0.85` 秒，**预警结束才打第一枪**——这是给玩家的反应窗口，不能省。
- `attack`：有视线且距离大于偏好距离就追击；靠得太近且有视线则后撤；射击前若冷却已低于 `0.6` 秒会再显示一次 `!` 预警，然后开火。
- `search`：走向最后目击位置，找不到路径时左右张望；超过记忆时长转回 `patrol` 并显示 `?` 气泡。
- `dead`：立即从可命中列表移除，再播放歪倒／缩小动画。

硬性要求：

- **所有射击必须经真实射线结算**，先检查遮挡，再按距离命中率掷骰，打偏时按距离加散布。**不允许随机直接扣血。**
- 开火前重新检查遮挡；近战检查距离与高差。
- 敌人之间保持轻微分离，避免整队堆成一个点。
- 敌人要有巡逻摇摆与短腿摆动，搜索时微微歪头。

## 8. 界面

中文主体配少量手写英文，纸色面板、歪斜边框、简洁几何图标，不把整幅场景洗成低饱和米色。共五个屏加一个错误页，用 `ui.showScreen(name)` 全局互斥切换。

### 准备页 `#menu`

以实际三维街区为背景。左侧品牌 `PAPER STRIKE` 与标题「纸上交锋」，副标「日光街区 · 训练场 RANGE 01」，一句简介，三条特性，以及：

- `#btn-start`「进入训练场」——**主入口，验收脚本依赖这个 id，不要改名或移动。**
- `#btn-net`「双人对战」。
- `#btn-mute-menu` 音效开关。

右侧是两把武器的装备卡：`data-weapon="smg"`「蜂鸟 · 冲锋枪 / 连射 · 30 发 · 20 伤害」、`data-weapon="sniper"`「长鸣 · 狙击枪 / 单发 · 5 发 · 100 伤害」。底部给操作提示条。

### HUD `#hud`

- 左上：任务名、剩余敌人数 `n / 8`、进度点。
- 右上：计时器、状态文字「交火中」，以及一行对手信息（含名字与血量，单人模式隐藏——**不要新增第五个角**，HUD 四角是绝对定位，多一个角就要在小窗口重排）。
- 顶部中间：暂停按钮、静音按钮。
- 中央：准星（四条线加中心点）、命中标记、爆头提示。
- 狙击镜：暗角 + 镜环 + 倍率标签，仅狙击枪开镜时显示。
- 左下：生命数值与血条。
- 右下：武器名、当前弹量、无限备弹符号 `∞`、换弹进度条与文字。
- 另需：受击暗角、漫画字效果层、提示层、兼容模式提示。

### 暂停 / 结算 / 错误

- 暂停 `#pause`：`继续演习` / `重新开始` / `返回准备页`。暂停冻结游戏时间与武器冷却，弹药与计时都不推进；战斗中的暂停弹窗与结算页必须互斥。
- 结算 `#result`：标题（胜利 / 失败）、副标题，以及击杀数、用时、命中率、命中／射击四项数据；`再来一次` / `返回准备页`。重开要**彻底**恢复初始状态。
- `#webgl-error`：创建 WebGL 上下文失败时的错误页，显示原因文本。

### 交互与输入

- 点击开始后请求 Pointer Lock，**取得控制前不刷新敌人攻击**。
- 拒绝或不可用时自动启用兼容模式：拖动鼠标或方向键观察、`F` 射击、`T` 瞄准，并给出提示。这个兼容分支贯穿输入处理，改动输入相关代码时它是第一个该检查的开关。
- 失焦、标签页隐藏、正常鼠标锁定丢失都应暂停。
- 音频仅在用户手势后启动。

## 9. 同局域网 1v1 对战

### 已定的四个前提

| 决策 | 选择 |
| --- | --- |
| 连接方式 | 两台电脑直连，各自渲染自己的视角，不做同屏分屏 |
| 对战形式 | 1v1 决斗，决斗模式场地清空，不要那 8 名 AI 敌人 |
| 信令 | **手动复制粘贴邀请码**，零基础设施，不引入后端或第三方服务 |
| 网络范围 | 同一局域网 → `iceServers: []`，零外部请求 |

### 传输层：先做假传输，再做真的

定义统一的 `Transport` 接口与三种实现，**网络会话必须只依赖这个接口，绝不直接 `new RTCPeerConnection`**：

- `createLoopbackPair({ latencyMs, jitterMs, lossRate })`——进程内回环，可注入延迟与丢包，供无 DOM 的单测使用。
- `createBroadcastTransport({ roomId, isHost, selfId })`——基于 `BroadcastChannel`，让**同一个浏览器的一个标签页就能跑通整套流程**，不需要 WebRTC、STUN 或第二台机器。
- `RtcTransport({ iceServers, gatherTimeoutMs, isHost })`——WebRTC 实现，是**唯一**允许出现 `new RTCPeerConnection` 的地方。

⚠️ **广播实现必须按角色（`host` / `guest`）寻址，不能按 `selfId` 寻址。** 调用方传了自定义 `selfId` 时，按 id 寻址会让两端永远对不上，消息被**静默丢弃**——状态照样是 `open`，只是什么都收不到。这是极难查的一类 bug。

传输层还要提供：状态机 `idle / gathering / offer-ready / answer-ready / connecting / open / failed / closed`；两条逻辑通道 `reliable` 与 `unreliable`；收发统计（`sent / recv / dropped / badPackets / sentBytes / recvBytes`）；以及给「复制诊断信息」按钮用的 `diagnosis()` 文本。**`disconnected` 不是失败**，它是常见且能自愈的中间状态，只有 `failed` 才是终态。

### 邀请码：让字符串活着从一台电脑到另一台电脑

这是整个联机部分最容易被低估的一环。微信 / QQ 会折行、加引号、把 `--` 变成 `—`、把全角半角搞混、在前后夹带「收到」「好的」。首次成功率是有限的，容错必须做在前面。

格式：`JSON.stringify` → `deflate-raw` 压缩（`CompressionStream`）→ **base64url**（`+`→`-`、`/`→`_`、去掉 `=`）→ 按 **64 列**折行 → 加前缀 `PS1-` 与后缀 `-END`。约 8 ~ 13 行，一条聊天消息发得下。

- 用 base64url 而不是 base64：`+` `/` `=` 会被 URL 处理或客户端吞掉。
- 前缀里的 `1` 是协议版本。版本不符要给「对方版本不同，请双方都刷新页面」的人话提示。
- **必须非 trickle（vanilla ICE）**：等 `iceGatheringState === 'complete'` 再交出 SDP。trickle 需要额外传候选，对复制粘贴的流程不可接受。超时设 `8000` 毫秒，超时就用现有候选并给出警告。⚠️ 等待前要先判断状态**是否已经是 `complete`**，它可能在监听器挂上之前就完成了。

解码必须容忍：折行、空格、全角字母数字、全角下划线、各种长得像减号的字符（`‐‑–―−﹘﹣－`）、零宽字符、被引号包起来、前后夹带聊天记录、整段加 `>` 引用符号。

⚠️ **剔除非 base64url 字符必须发生在查找后缀之前。** 否则只要任何非字母表字符落在 `-END` 中间就解不出来——而折行正好把 `-END` 劈成两行、聊天软件又在折行处插了 `> ` 时，就会变成 `-` + `> ` + `END`，永远匹配不上。这个 bug **是数据相关的**，同一段码有时能解有时不能，极难查。必须写一条**穷举每一个折行位置**的回归测试，不能只碰运气试一个。

任何解析失败一律返回 `null`，**绝不抛异常**——用户粘进来的东西什么都有可能。超长输入要在解压**之前**就拒绝，否则它本身就是个放大器。

### 一个会让「复制」按钮直接失效的坑

⚠️ `navigator.clipboard` 只在**安全上下文**里存在。`http://127.0.0.1` 算安全上下文，但 **`http://192.168.x.x` 不算**——也就是用户选定的「局域网两台电脑」这条路上，`navigator.clipboard` 直接是 `undefined`。

所以复制按钮必须有回退链：`navigator.clipboard.writeText` → `textarea.select()` + `document.execCommand('copy')` → 选中文本并**明确告诉用户手动按 Ctrl+C**。不做回退的话，这个按钮在唯一真正需要它的场景里是坏的。

（`CompressionStream` 和 `RTCPeerConnection` **不受**安全上下文限制，局域网 http 下都能用。）

### 对战面板 `#net`

内部若干步骤互斥切换，**不新增第五个 screen**：

1. **选身份**：`我是房主 · 生成邀请码` / `我是挑战者 · 我有邀请码`。另有一个可选的「STUN 地址」输入框，默认留空。
2. **房主**：显示待发出的邀请码 + 复制按钮；下方粘贴对方回传的应战码 + 应用按钮。
3. **挑战者**：粘贴收到的邀请码 + 生成应战码；再显示待发回的应战码 + 复制按钮。
4. **已连接**：昵称、延迟毫秒数、候选类型，以及「进入决斗场地」按钮。

底部常驻一行人话状态与「复制诊断信息」「返回准备页」。

- **界面必须始终显示当前处于哪一步**，否则失败时用户不知道卡在哪。
- 连接失败要给**分类提示**（区分「一条候选都没收到」「只有内网地址」「有公网候选但打不通」），不要只说「连接失败」——用户需要知道下一步该干什么。
- **没点「我是房主 / 我是挑战者」之前，绝不 `new RTCPeerConnection`。** 回到准备页要把连接断掉，留着的 PeerConnection 会继续做网络探测。
- **不要在代码里硬编码任何 STUN / TURN 地址。** 地址由使用者自己填，且只接受 `stun:` / `stuns:` 开头的值，避免这个输入框变成「页面往任意地址发请求」的口子。默认留空即零外部请求。

### 两台电脑怎么打开同一个页面

1. **线上静态部署（首选，零配置）**：双方各自打开公网地址即可。HTTPS 是安全上下文，`navigator.clipboard` 可用。
2. **局域网 dev server**：加一个 `dev:lan` 脚本（等价于 `vite --host`）。⚠️ **这会暴露给整个局域网**，且开发钩子能直接改血量、传送、结算胜负，仅限可信网络；Windows 还会弹防火墙授权。**不要改默认配置的 `127.0.0.1` 绑定**——局域网必须是显式选择。注意这条路上 clipboard 不可用（见上）。

**必须接受**：无 STUN/TURN 时只有 host candidate，WebRTC **只能同局域网**。这不是实现缺陷，是「不引入任何第三方服务」的直接代价，要在界面上说明白。

## 10. 工程与验收

### 交付物

提供 `package.json`、锁文件、`README.md` 与源码，支持：

| 命令 | 用途 |
| --- | --- |
| `npm ci` | 按锁文件安装 |
| `npm run dev` | 本地开发服务器，**只监听 `127.0.0.1`** |
| `npm run dev:lan` | 开发服务器，监听所有网卡供局域网访问 ⚠️ |
| `npm test` | Node 单元／逻辑测试 |
| `npm run build` | 构建到 `dist/` |
| `npm run preview` | 本地预览 `dist/` |
| `npm run check:publish` | 扫描待提交内容里的敏感数据 |

产物为 `dist/` 静态站点，可由任意静态 HTTP 服务托管。**不能通过双击开发用 `index.html` 运行 ES Modules 工程。** 安装依赖需要网络；游戏本身不需要 API key、环境变量或后端服务。

建议的源码划分（**逻辑模块必须能在无 GPU、无 DOM 的环境下测试**）：

```
src/
  main.js           渲染循环、游戏状态与各模块的接线
  world.js          关卡、碰撞、导航、场景与描边
  sketch.js / textures.js   固定种子手绘工具和 Canvas 贴图
  player.js         视角、移动、跳跃与碰撞
  input.js          输入采样；本地 / 远端 / 脚本三种输入源
  loadout.js        一个持枪者的全部状态：武器、弹药、后坐力、HUD
  match.js          角色注册、模式规则、胜负判定（无 DOM）
  player-rig.js     远端玩家的可视化身与动画
  weapon-state.js   弹量、冷却、换弹与伤害逻辑（与持有者无关）
  weapons.js        第一人称纸板枪、动作与独立渲染
  enemies.js        角色、AI、寻路与伤害
  combat.js         射线判定与特效
  audio.js          Web Audio 合成音效
  ui.js / style.css 界面与草图风样式
  net-panel.js      对战面板：邀请码流程与连接状态
  net/              protocol.js / transport.js / rtc-transport.js / signaling.js
```

### 架构要求

- **单一角色状态与持有者解耦**：武器状态、后坐力、统计、换弹进度都必须「每个持枪者一份」，不能是模块级全局。这是让单人模式、决斗模式、以及未来的远端玩家能共用同一套代码的前提。
- **胜负判定集中一处**，按模式分支：单人看存活敌人数，决斗看两队是否都还有人。不要在渲染循环里散落 `if (mode === ...)`。
- 复用同一份物理积分与同一份命中判定，让本地玩家、敌人、远端角色自动获得一致的手感与穿墙保护。
- 模式切换要**彻底重建**：单人 → 决斗 → 单人的来回切换后，敌人、HUD 引用、对手状态都必须复位干净。这一条要写测试。
- ⚠️ 决斗模式把场地清空时，不能让「存活敌人数为 0」直接触发单人模式的胜利条件——模式分支必须在场地清空**之前**就位。

### 测试

`npm test` 用 `node --test`，当前规模 **152 项**（149 项游戏逻辑 + 3 项发布扫描）。必须覆盖：

十项以上的战斗与物理测试之外，重点覆盖：武器冷却与换弹、切枪保留弹量、伤害与射线遮挡、爆头倍率、玩家碰撞与台阶、导航连通性、八个出生点到入口与高台的路径、**两个决斗出生点的合法性／对称性／互不可见／可互相走到**、输入抽样与边沿消费、角色注册与胜负判定、模式往返后的引用复位、报文字段与版本门槛、传输接口（含「传了自定义 `selfId` 也照样收得到」的回归）、邀请码编解码的全部容错分支。

### 浏览器验收

仓库自带基于**原生 CDP + WebSocket** 的验收脚本，零额外依赖（不需要 Playwright），自动在系统标准位置找 Chrome / Edge。两套：

| 脚本 | 断言数 | 证明什么 |
| --- | --- | --- |
| `tests/browser-qa.js` | 37（`--prod` 时 9） | 战斗逻辑、HUD、胜负分支、几何、对战模式、离线保证、控制台无错 |
| `tests/net-qa.js` | 28 | 两个标签页建链、邀请码容错、面板全流程 |

```bash
npm run dev
node tests/browser-qa.js http://127.0.0.1:5173/ artifacts
node tests/net-qa.js    http://127.0.0.1:5173/ artifacts
npm run build && npm run preview
node tests/browser-qa.js http://127.0.0.1:4173/ artifacts --prod
```

要求：

- 截图与 JSON 报告输出到指定目录，**不入 Git**。
- ⚠️ **WebRTC 走 UDP，CDP 的 `Network.requestWillBeSent` 看不见它。** 因此「无跨源第三方请求」这条断言在引入联机之后**不再能证明页面是离线**的。替代品是一条新断言：**未打开对战面板时全程不实例化 `RTCPeerConnection`**（用一个注入的哨兵计数）。「单人模式仍然完全离线」必须依然是个可证命题。
- 生产冒烟要额外验证**优化构建里对战代码真的能用**（能打开面板并生成一个 `PS1-` 开头的邀请码），而不是被 tree-shaking 顺手摇掉；同时验证**开发钩子入口已被移除**。
- ⚠️ 验收脚本点按钮前要确认目标元素**真的可见**。隐藏元素的包围盒是零尺寸，合成点击会落在 `(0,0)` **且不报错**——这类静默失效极难查。
- ⚠️ 验收脚本里「等某个表达式为真」的返回值是那个表达式本身（`true`/`false`），不是被读的值。要取值就再读一次。

**必须如实报告**：验收脚本用开发钩子直接设位置、直接结算胜负，**不等同于用真实鼠标完整清场一次**；决斗那几条走的是钩子起局，**不等于两人各坐一台电脑真打一局**；联机那 28 项跑的是**同一台机器上的两个标签页**，**不证明跨机器、跨防火墙、mDNS 环境下能连上**。真实手感、画面观感、跨机连通性都只能人工确认，不要把自动化全绿说成这些已验证。

### 安全与提交范围

- **绝不要把真实抓包的邀请码 / 应战码提交进仓库。** 那段字符串解出来就是完整 SDP，里面有 `c=IN IP4 <你的局域网IP>` 这样的候选地址，等于把内网拓扑写进公开 Git 历史。测试夹具一律用合成数据，地址用 RFC 5737 保留给文档的 `192.0.2.0/24`。
- **绝不硬编码 STUN / TURN 地址**（见第 9 节）。
- `VITE_*` 环境变量会被编译进前端，**绝不能用来保存秘密**。
- 发布只包含源码、公开文档、测试、锁文件及工作流。`.gitignore` 应排除本机环境文件、包管理器认证配置、私钥、依赖目录、构建目录、浏览器截图与验收报告。提供一个扫描脚本检查常见令牌／私钥格式、授权头、带凭据 URL、硬编码密钥与个人目录路径，命中时**只打印文件名、行号和类型，不回显可疑值**。规则扫描不是穷尽性安全保证，仍需人工审查提交清单。

最终交付可运行源码、启动方式及**如实的**验证结果。全部要求均已在本文给出，不需要索取外部视觉参考文件。
